# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: prometheus-data-pvc
#   namespace: {{ .Values.namespace }}
# spec:
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: {{ .Values.prometheus.pvc.dataStorage }}

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-data-pvc
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: openebs-cstor-repl2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.prometheus.pvc.dataStorage }}
