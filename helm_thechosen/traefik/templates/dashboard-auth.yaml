apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-auth
  namespace: {{ .Values.namespace }}
spec:
  basicAuth:
    secret: dashboard-auth-secret  # Utilise le secret 

  # ingressRoute:
  #   dashboard:
  #     enable: true

